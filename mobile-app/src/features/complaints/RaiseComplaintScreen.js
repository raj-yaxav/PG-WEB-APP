import { ScreenShell, PlaceholderBox } from '../../shared/components/ScreenShell';

export function RaiseComplaintScreen() {
  return (
    <ScreenShell title="Raise Complaint">
      {/*
        UI TODO:
        - Complaint category dropdown.
        - Title input.
        - Description input.
        - Image upload optional.
        - Submit complaint button.
        - Success/error message.
      */}
      <PlaceholderBox text="Raise Complaint UI will be designed later." />
    </ScreenShell>
  );
}
