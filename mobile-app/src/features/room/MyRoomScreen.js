import { ScreenShell, PlaceholderBox } from '../../shared/components/ScreenShell';

export function MyRoomScreen() {
  return (
    <ScreenShell title="My Room">
      {/*
        UI TODO:
        - Show property name.
        - Show room number.
        - Show bed number.
        - Show room type.
        - Show rent amount.
        - Show facilities.
        - Show joining date.
        - Show manager contact.
      */}
      <PlaceholderBox text="My Room UI will be designed later." />
    </ScreenShell>
  );
}
